{
  "name": "@umbrella/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx src/server.ts",
    "start": "tsx src/server.ts",
    "build": "npx tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'No lint rules configured for @umbrella/web yet.'",
    "test": "tsx --test src/**/*.test.ts"
  },
  "dependencies": {
    "@umbrella/channel-config": "0.1.0",
    "@umbrella/ui": "0.1.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
